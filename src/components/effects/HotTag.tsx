export function HotTag() {
  return (
    <span className="relative inline-flex items-center gap-1 text-[10px] font-bold uppercase tracking-wider text-error bg-error/15 px-2 py-0.5 rounded-full border border-error/20">
      <span className="w-1.5 h-1.5 rounded-full bg-error animate-pulse" />
      HOT
    </span>
  );
}
